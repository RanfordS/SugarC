// comment

Int: a = 1 ? 2+2 : 3;

Function Float: foo (Int: bar)
{
    a <<= +1;
	//	non ascii characters, such as ランフォード,
	//	are not permitted outside of comments or strings
	return bar + 1.0f;
}

Function Float: quadratic (a, b, c, x)
{
    return a*x*x + b*x + c;
}

/*	Standard main file definition,
	nothing much here */
Function Int: main (argc, &&Char: argv)
{
    for (Size: i = 0; i < 5; ++i)
    {
        io.printf ("Ping %i\n", i);
    }

	io.printf ("Hello \"world\"\n");
	return 0;
}

// Considerations for function definitions

Float (Int): foo (bar) =
{
    a <<= +1;
    return bar + 1.0f;
}

Float (4): quadratic (a, b, c, x) =
{
    return a*x*x + b*x + c;
}

Int (1, &&Char): main (argc, argv) =
{
    for (Size: i -> 5)
    {
        io.printf ("Ping %i\n", i);
    }

	io.printf ("Hello \"world\"\n");
	return 0;
}

// ^ no... actually awful

// end of file
